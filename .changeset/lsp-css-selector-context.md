---
"@rsvelte/language-server": patch
---

Complete a CSS selector position the way the official service does: pseudo-classes, `:global()`, pseudo-elements, every HTML and SVG element name, and — where a statement may begin — the at-directives, each with the entry's documentation, its `tags`, the vendor-prefix `sortText` and a snippet stop inside a trailing `()`. Previously only a single `:global` item was offered there.
